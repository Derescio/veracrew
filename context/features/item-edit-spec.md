# Item Drawer — Edit Mode

## Overview

Adding and Editing Items

## Requirements


- On Large screens the new item type dropdown UI needs improving. You have to mouse over the words to see the text. That needs to be fixed

- If context is too big allow the user to scroll. Right now you have to press the tab button to save the item.

- For items that are PRO dont show them in the when adding a new item drop down if the user is not a PRO user

- In the Dashboard, disable items that are PRO. Only Pro users should be able to select


### Reference

/dashboard
/items/[item-type]